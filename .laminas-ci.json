{
    "additional_checks": [
        {
            "name": "Backend Linting",
            "job": {
                "command": "composer lint:php-cs && composer lint:phpstan && composer lint:twig && composer lint:rector",
                "php": "8.4"
            }
        }
    ]
}
