{% extends 'base.html.twig' %}

{% block title %}Qr code!{% endblock %}

{% block body %}

    <div class="container mt-5 mb-5 text-center">
        <h2 class="h3 font-weight-normal">Scan Qr Code :</h2>
        <div class="mt-3 mb-3">
            <img src="data:image/png;base64,{{ qrCodeImage }}"/>
        </div>
        <a class="btn btn-lg btn-primary" href="{{ path('app_security_validate_authentification') }}">Continue</a>
    </div>

{% endblock %}
